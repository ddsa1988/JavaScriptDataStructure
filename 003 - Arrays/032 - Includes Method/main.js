"use strict";

const numbers = [1, 2, 3, 4, 15, 3, 10];

console.log(numbers.includes(4));
console.log(numbers.includes(2, 3)); //We can pass a starting index we want the array to start searching for the value
console.log(numbers.includes(400));
