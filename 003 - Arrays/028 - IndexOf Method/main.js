"use strict";

//The indexOf method return the index of the first element that matches the argument passed
const numbers = [1, 2, 3, 4, 5, 3];

console.log(numbers.indexOf(3));
console.log(numbers.indexOf(10));
